# Ready to run

This folder is reset for a fresh study. It contains no ledgers, logs, insights,
summaries, or result plot.

Both researcher proposal files exactly match `initial_experiment.py`. Therefore
round 1 for both circuits will use the same fixed FP64 TensorNet settings.
Follow the copy-and-paste agent prompt in `README.md` to begin.
